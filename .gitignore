# Private credentials — must never be published
service_account.json
**/service_account.json
connection.json
**/connection.json
.env
mac.zip
# Generated copy of the Apps Script with the key already substituted in
PASTE-THIS-INTO-APPS-SCRIPT.gs

# Real work orders — contain customer names, phones and addresses.
# Keep them locally for testing; never publish them.
samples/*.pdf
samples/*.png
samples/*.jpg
samples/*.jpeg

# Editor / assistant local settings
.claude/

# Generated installer (built by tools/make_mac_installer.py and by CI)
mac/Install SHEET auto FILL.command
mac/Install SHEET auto FILL.command.zip

# Build output (large, rebuilt by CI)
build/
dist/
App/
*.spec.bak
selftest.log
*.bak
.asset_version

# Python / OS noise
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
Thumbs.db
