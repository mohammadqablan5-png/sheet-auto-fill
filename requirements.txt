# Everything here is free and runs locally — no accounts, no API keys, no usage fees.
flask>=3.0
pyyaml>=6.0

# Google Sheets writing (free service-account access)
gspread>=6.0
google-auth>=2.30

# Reading PDFs: text layer first, then offline OCR for image-only PDFs
pdfplumber>=0.11
pypdf>=4.0
pypdfium2>=4.30
rapidocr-onnxruntime>=1.3
pillow>=10.0
numpy>=1.26

# Native app window
pywebview>=5.0
pyobjc-core>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-WebKit>=10.0; sys_platform == "darwin"
